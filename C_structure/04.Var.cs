using System;

class Var
{
    static void Main()
    {
        var name = "c#"; // string 형식으로 변환
        Console.WriteLine(name);

        var version = 8.0; // double 형식으로 변환
        Console.WriteLine("{0:0.0}", version);
    }
}